# CSV可视化工具 - 时间筛选和滑块控制改进计划

## [x] 任务1：修复滑块控制松手回弹问题
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 修复X轴滑块控制在松手后回弹的问题
  - 确保滑块拖动后能够保持在用户指定的位置
  - 优化滑块事件处理逻辑
- **成功标准**：
  - 滑块拖动后松手不会回弹到原位置
  - 滑块能够正确调整X轴范围
  - 图表能够根据滑块位置正确更新
- **测试要求**：
  - `programmatic` TR-1.1：拖动滑块后，图表数据范围应正确更新
  - `human-judgement` TR-1.2：滑块拖动过程平滑，松手后位置稳定
- **备注**：参考原始版本的滑块实现逻辑，确保事件处理正确

## [/] 任务2：导入时间筛选文件后自动选择第一个有效的时间
- **优先级**：P1
- **依赖**：无
- **描述**：
  - 当用户导入时间筛选文件后，自动选择第一个有效的时间值
  - 确保选择的时间值显示在时间选择下拉框中
  - 提供视觉反馈，让用户知道已自动选择了时间
- **成功标准**：
  - 导入时间筛选文件后，时间选择下拉框自动选中第一个有效时间
  - 时间选择下拉框显示选中的时间值
  - 用户可以看到已选择的时间
- **测试要求**：
  - `programmatic` TR-2.1：导入时间筛选文件后，时间选择下拉框应自动选中第一个有效时间
  - `human-judgement` TR-2.2：时间选择下拉框应清晰显示选中的时间值
- **备注**：需要考虑时间筛选文件中可能存在的无效时间值

## [ ] 任务3：把选择的时间填写到X轴范围的开始和结束时间控件
- **优先级**：P1
- **依赖**：任务2
- **描述**：
  - 当用户从时间筛选文件中选择时间后，自动计算该时间+15分钟的范围
  - 将计算出的开始时间和结束时间填写到X轴范围的开始时间和结束时间控件中
  - 确保时间格式正确，符合控件要求
- **成功标准**：
  - 选择时间后，X轴范围的开始时间和结束时间控件应自动填写
  - 开始时间为选择的时间，结束时间为选择的时间+15分钟
  - 时间格式正确，能够被控件识别
- **测试要求**：
  - `programmatic` TR-3.1：选择时间后，开始时间和结束时间控件应自动填写正确的时间值
  - `human-judgement` TR-3.2：时间值应清晰显示在控件中，格式正确
- **备注**：需要处理时间格式的转换，确保与控件要求匹配

## [ ] 任务4：当X轴选择时间列时，Y轴不显示时间列
- **优先级**：P2
- **依赖**：无
- **描述**：
  - 当用户在X轴选择时间列时，自动从Y轴和Y轴2的选项中移除时间列
  - 确保Y轴和Y轴2的选择器不包含时间列
  - 当X轴切换到非时间列时，恢复时间列到Y轴选项中
- **成功标准**：
  - X轴选择时间列后，Y轴和Y轴2的选择器中不显示时间列
  - X轴切换到非时间列后，Y轴和Y轴2的选择器中恢复显示时间列
  - 时间列在Y轴选择器中的显示/隐藏逻辑正确
- **测试要求**：
  - `programmatic` TR-4.1：X轴选择时间列后，Y轴和Y轴2选择器应不包含时间列
  - `programmatic` TR-4.2：X轴切换到非时间列后，Y轴和Y轴2选择器应恢复包含时间列
- **备注**：需要识别哪个列是时间列，可能需要基于列名或内容进行判断