# CSV可视化工具 - UI改进计划

## [x] 任务1：减少编码分析结果示例的显示空间
- **优先级**：P2
- **依赖**：无
- **描述**：
  - 减少编码分析结果示例的显示空间，使其更加紧凑
  - 调整预览区域的高度和样式，减少垂直空间占用
- **成功标准**：
  - 编码分析结果示例显示更加紧凑，不占用过多垂直空间
  - 预览内容仍然清晰可见，能够帮助用户判断编码是否正确
- **测试要求**：
  - `programmatic` TR-1.1：编码预览区域高度明显减少
  - `human-judgement` TR-1.2：预览内容清晰可见，同时不占用过多空间

## [x] 任务2：实现选择后立即生效（画图除外）
- **优先级**：P1
- **依赖**：无
- **描述**：
  - 修改各种选择控件，使其在输入或选择后立即生效，不需要额外的确认按钮
  - 保持画图功能需要点击按钮才生效的行为
- **成功标准**：
  - 时间选择、编码选择等控件在选择后立即生效
  - 画图功能仍然需要点击按钮才生效
- **测试要求**：
  - `programmatic` TR-2.1：时间选择后立即更新相关控件
  - `programmatic` TR-2.2：编码选择后立即更新预览
  - `human-judgement` TR-2.3：用户操作流程更加流畅，不需要额外的确认步骤

## [/] 任务3：改进X轴拖动条行为
- **优先级**：P1
- **依赖**：无
- **描述**：
  - 修改X轴拖动条，使其以X轴开始结束时间输入框为两端的约束
  - 不按照滑动后的位置重置滑动条的范围
  - 设置滑动后的滑块间的最小距离为X轴范围的30分之一
- **成功标准**：
  - X轴拖动条的范围由时间输入框决定，不会被滑动操作重置
  - 滑块间的最小距离为X轴范围的30分之一
- **测试要求**：
  - `programmatic` TR-3.1：修改时间输入框后，滑块范围相应更新
  - `programmatic` TR-3.2：滑动滑块后，滑块范围保持不变
  - `programmatic` TR-3.3：滑块间的最小距离为X轴范围的30分之一

## [ ] 任务4：修复表格显示行数问题
- **优先级**：P1
- **依赖**：无
- **描述**：
  - 彻底修复数据表格显示行数不正确的问题
  - 确保表格显示的行数符合用户设置的每页显示数量
- **成功标准**：
  - 数据表格显示的行数正确，符合用户设置
  - 分页功能正常工作
- **测试要求**：
  - `programmatic` TR-4.1：表格显示的行数符合用户设置的每页显示数量
  - `programmatic` TR-4.2：分页功能正常，能够正确切换页面
  - `human-judgement` TR-4.3：表格显示清晰，行数正确

## [ ] 任务5：重新布局表格控件
- **优先级**：P2
- **依赖**：无
- **描述**：
  - 重新布局表格上的控件，使其显示在一行
  - 确保控件文字不要显示为2行
- **成功标准**：
  - 表格控件显示在一行，布局紧凑
  - 控件文字显示为一行，不换行
- **测试要求**：
  - `programmatic` TR-5.1：表格控件显示在一行
  - `human-judgement` TR-5.2：控件布局紧凑，文字不换行
