# CSV文件可视化分析工具

## 项目简介

CSV文件可视化分析工具是一个基于浏览器的数据分析工具，支持上传、解析和可视化各种格式的数据文件，特别是CSV、DAT、TXT和Excel文件。该工具具有强大的编码检测和处理能力，能够正确处理GB2312、GBK和UTF-8等多种编码格式的文件。

## 主要功能

### 1. 文件上传和解析
- 支持CSV、DAT、TXT和Excel文件格式
- 支持自动检测和手动选择文件编码
- 支持Excel文件的多个Sheet选择和合并
- 支持TAB、逗号和空格作为分隔符

### 2. 编码处理
- 自动检测文件编码（GB2312、GBK、UTF-8等）
- 支持手动选择编码格式，放在文件名下方便于操作
- 使用浏览器内置的TextDecoder API进行准确的编码转换
- 保留备用解码方案，确保在各种浏览器环境下都能正常工作

### 3. 数据处理
- 自动检测和合并多行行标题，去掉多余空格
- 智能识别时间值和数据值
- 支持零列过滤功能，可选择是否过滤全为0的列
- 支持数据表格显示和分页
- 支持原始数据模式下的时间筛选

### 4. 可视化功能
- 支持绘制折线图，可选择X轴和Y轴
- 支持多Y轴数据同时显示
- 支持调整X轴时间范围，使用现代化滑块控件
- 支持导出图表为图片

### 5. 时间筛选
- 支持上传DemControl时间筛选文件
- 可选择筛选起始时间
- 筛选数据文件中所选时间+15分钟范围内的数据
- 原始数据模式下也应用时间筛选

### 6. 开发规范
- 内置dev-standard开发规范SKILL
- 代码备份、需求文档修订、功能测试、版本维护一体化流程
- 支持Git和压缩归档两种备份方式

## 如何使用

1. **上传数据文件**：点击左侧的"数据文件导入"区域，上传DemDec格式的数据文件
2. **选择编码**：如果文件显示乱码，可以在文件名下方的编码选择器中选择正确的编码格式
3. **选择Excel工作表**：如果上传的是Excel文件，可以选择要解析的工作表或合并多个Sheet
4. **配置坐标轴**：在"折线图坐标轴配置"区域选择X轴和Y轴
5. **绘制图表**：点击"绘制折线图"按钮生成图表
6. **调整时间范围**：使用现代化滑块控件调整X轴范围来缩放图表
7. **导出图表**：点击"导出图表为图片"按钮保存图表
8. **时间筛选**：如果需要按时间筛选，可以上传DemControl时间筛选文件并选择起始时间

## 支持的文件格式

- **CSV文件**：逗号分隔值文件
- **DAT文件**：数据文件，支持多种分隔符
- **TXT文件**：文本文件，支持多种分隔符
- **Excel文件**：.xlsx和.xls格式，支持多个Sheet和Sheet合并

## 技术实现

- **前端框架**：纯HTML、CSS和JavaScript
- **构建工具**：Vite
- **UI框架**：Bootstrap 5
- **图表库**：Chart.js
- **Excel解析**：xlsx.js（本地库，提高离线可用性）
- **编码处理**：浏览器内置的TextDecoder API
- **文件处理**：FileReader API

## 版本更新记录

### [1.8.9] - 2026-07-13
- 修复左右侧Y轴选择不同数量数据列时，只按较少数量画图的问题

### [1.8.8] - 2026-07-10
- 新增CSV格式支持：时间列在第二列的格式（第一列省略）

### [1.8.7] - 2026-07-08
- 修复GitHub Pages部署后XLSX库无法加载的问题
- 将lib目录迁移到Vite public目录

### [1.8.6] - 2026-07-08
- 修复无标题空格分隔文件读入后数据未更新的问题
- 修复文件名日期解析正则表达式匹配错误

### [1.8.5] - 2026-07-08
- 差值计算设置选择"原始值"时，左侧和右侧选定的数据分别绘制原始数据曲线
- 选择"1阶/2阶/3阶差值"时，计算右侧-左侧的差值并绘制差值曲线

### [1.8.4] - 2026-07-08
- 新增差值计算功能：支持原始值、1阶差值、2阶差值、3阶差值
- 单侧数据差值计算：相邻数据前后计算差值
- 双侧数据差值计算：左右侧对应列数据计算差值（右侧-左侧）
- 导出数据时自动添加差值列

### [1.8.1] - 2026-07-01
- 图表导出文件名优化、图例导出、下划线时间格式支持
- 无标题行文件处理修复、自动化测试Agent创建

> 查看完整版本更新记录，请参考 [CHANGELOG.md](./CHANGELOG.md)

## 运行方式

### 开发模式
```bash
npm install
npm run dev
```
然后在浏览器中访问 http://localhost:5173

### 生产构建
```bash
npm run build
```

### 预览构建结果
```bash
npm run preview
```

### 部署到GitHub Pages
```bash
npm run deploy:github
```

## 注意事项

- 对于大型文件，解析可能需要一些时间
- 建议使用现代浏览器（Chrome、Firefox、Edge等）以获得最佳体验
- 如果遇到编码问题，可以尝试手动选择编码格式
- Excel文件的解析依赖于xlsx.js库，较大的Excel文件可能需要更长的解析时间
- 项目已配置国内npm镜像源，可提高依赖安装速度

## 联系方式

如有问题或建议，请联系项目维护者。
